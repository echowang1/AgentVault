version: '3.8'

services:
  mpc-wallet:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - MPC_API_KEYS=test-api-key
    volumes:
      - ./data:/app/data
